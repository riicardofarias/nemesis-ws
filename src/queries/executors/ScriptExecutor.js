module.exports = class ScriptExecutor{
    constructor(endpoint){
        this.endpoint = endpoint;
    }

    run(){
        try{
            
        }catch(err){
            console.log(err);
        }
    }
}